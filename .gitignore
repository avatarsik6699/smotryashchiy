# Go
/bin/
/dist/
/data/
*.db
*.db-wal
*.db-shm

# Secrets — never commit
.env
*.key
secrets*.json

# Frontend
web/node_modules/
web/dist/

# Local agent state
.claude/settings.local.json

# Built web UI (only .gitkeep is tracked so `go build` works without node)
web/dist/*
!web/dist/.gitkeep
web/node_modules/
